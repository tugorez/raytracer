class Canvas {
  final int width;
  final int height;

  Canvas({
    required this.width,
    required this.height,
  });

  @override
  String toString() => 'Canvas(width: $width, height: $height)';
}
